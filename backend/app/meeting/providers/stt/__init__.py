"""STT sub-package — deprecated, retained for import compatibility.

The stt/ sub-package is no longer active. Speech processing is handled
exclusively by the providers/speech/ package (DeepgramSpeechProvider).

This __init__.py is intentionally empty to prevent ImportError from
any legacy code that may import from this path.
"""
